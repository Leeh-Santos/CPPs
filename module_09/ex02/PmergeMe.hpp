# pragma once

# include <iostream>
# include <vector>
#include <cstdlib>
#include <algorithm>

class PmergeMe
{
private:
    
public:
    PmergeMe(/* args */);
    ~PmergeMe();
};

void checkInput( std::vector<std::string> &args);